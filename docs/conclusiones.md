# Guía de Uso

Esta guía te mostrará cómo poner en marcha y utilizar la **Calculadora Python**. Esta calculadora funciona como una aplicación de escritorio con una interfaz gráfica (GUI).

---

## 🚀 Requisitos

Antes de empezar, asegúrate de tener lo siguiente:

* **Python 3:** La aplicación está escrita en Python 3.
* **Bibliotecas:** Es posible que necesites instalar algunas bibliotecas gráficas (como Tkinter, PyQt, Kivy, etc.). Si es así, puedes añadir un archivo `requirements.txt`.

```bash
# Instala las dependencias necesarias
pip3 install -r requirements.txt
